[phases.setup]
aptPkgs = ["build-essential", "curl"]

[phases.install]
cmds = ["pip install -r requirements_minimal.txt"]

[start]
cmd = "uvicorn test_minimal_api:app --host 0.0.0.0 --port $PORT"

[variables]
PYTHON_VERSION = "3.10"

[nixpacks]
start-command = "uvicorn test_minimal_api:app --host 0.0.0.0 --port $PORT"
