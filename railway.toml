[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn morvo_api_minimal:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
