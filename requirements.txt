# MINIMAL WORKING REQUIREMENTS - Railway Deployment Fix
# Date: 2025-05-31 - Guaranteed to work without problematic dependencies

# Core Framework (Essential)
fastapi==0.115.12
uvicorn==0.34.2
python-dotenv==1.1.0
python-multipart==0.0.20

# Database & ORM (Essential)
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.16.1

# Authentication (Essential)
python-jose[cryptography]==3.5.0
passlib==1.7.4
pyjwt==2.10.1

# HTTP & Utilities (Essential)
requests==2.32.3
pydantic==2.11.5

# Production Server
gunicorn==21.2.0

# Basic AI (Core only - no problematic tools)
openai==1.75.0
langchain-core==0.3.0
langchain-openai==0.2.0

# Background Tasks (Optional - comment out if not needed)
celery==5.5.2
redis==6.2.0

# Supabase (if using)
supabase==2.15.2
postgrest==1.0.2

# TEMPORARILY REMOVED TO FIX DEPLOYMENT:
# crewai>=0.121.0  # This might be pulling in google-analytics-reporting
# crewai-tools>=0.46.0  # This is likely the culprit
# langchain>=0.3.0  # Full langchain has many dependencies
# langchain-community>=0.3.0  # Community tools often problematic
# tweepy>=4.14.0  # Social media APIs
# facebook-sdk>=3.1.0
# python-linkedin>=1.9.0
# google-api-python-client>=2.100.0
# aioredis>=2.0.1

# NOTE: Add back packages one by one after successful deployment
